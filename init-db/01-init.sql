-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- This file runs automatically when the PostgreSQL container starts for the first time.
-- Add any initial schema or seed data here.

-- Example: Create schema (optional, JPA can handle this with ddl-auto=create)
-- The actual schema will be managed by Spring Data JPA / Flyway migrations
